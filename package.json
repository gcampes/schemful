{
  "name": "schemful-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "test": "pnpm --filter schemful test:run",
    "test:integration": "pnpm --filter schemful test:integration",
    "type-check": "pnpm -r type-check",
    "clean": "pnpm -r clean"
  },
  "packageManager": "pnpm@10.7.0",
  "engines": {
    "node": ">=18"
  }
}
